【00:00 - 00:06】
面试官如果问你my cycle如何减少回表，你千万不要只说一句加索引，这个回答太浅了。

【00:06 - 00:12】
真正要讲清楚得先说明白什么是回表，为什么回表慢，以及我们到底有哪些手段可以减少回表。

【00:12 - 00:15】
我是小哲，点赞、收藏加关注，我们马上开始。

【00:15 - 00:17】
更多内容可以查看橱窗。

【00:18 - 00:28】
先说什么是回表，在indate里面，主键索引存的是整行数据，但是普通索引也就是二级索引叶子节点里面存的不是整行数据，而是索引字段加主键值。

【00:29 - 00:45】
比如有一张用户表，主键是用户ID，普通索引是手机号，当你根据手机号查用户信息时，my co会先走手机号索引，找到对应的用户ID但如果你要查的字段不在手机号索引里面，m还得拿着这个用户ID再去主件索引里面查一次完整数据。

【00:45 - 00:48】
这第二次查主件索引的过程就叫回表。

【00:49 - 00:50】
那为什么要减少回表？

【00:50 - 01:00】
因为回表本质上就是多查一次索引数，一次查询如果只命中几条数据问题不大，但如果一次查几千条、几万条数据，每一条都要回表，那成本就会非常高。

【01:01 - 01:06】
尤其是分页查询、范围查询、列表查询里面，回表次数一多，性能就会明显下降。

【01:06 - 01:12】
所以优化目标很简单，让查询尽量在二级索引里就完成，少去主件索引，拿整行数据。

【01:13 - 01:17】
第一种方法使用覆盖索引，这是最常见也是最核心的方式。

【01:18 - 01:22】
所谓覆盖索引就是查询需要的字段都已经包含在索引里面。

【01:23 - 01:26】
比如你有一个联合索引，手机号、用户名状态。

【01:26 - 01:33】
如果你的cycle只查这几个字段，手机号、用户名、状态，那my直接在这个索引里就能拿到结果，不需要再回表。

【01:34 - 01:44】
所以面试里可以这样说，如果业务只需要展示列表页的少量字段，就不要直接查整行数据，而是把高频查询字段设计进联合索引，让它形成覆盖索引。

【01:44 - 01:49】
第二种方法，不要随便写查询全部字段，很多回表问题其实是自己写出来的。

【01:50 - 01:57】
比如明明页面只需要用户ID昵称、头像，你却直接查询所有字段，这样就算命中了普通索引，也大概率要回表。

【01:57 - 02:10】
所以减少回表的一个基本原则就是需要什么字段就查什么字段，不要为了省事儿把大字段、无关字段一起查出来，尤其是文本字段、备注字段、扩展字段，这些字段最好不要出现在高频列表查询里。

【02:11 - 02:14】
第三种方法，合理设计联合索引，覆盖索引。

【02:14 - 02:21】
不是说把所有字段都塞进索引，索引字段太多也会带来写入成本、存储成本和维护成本。

【02:21 - 02:25】
正确做法是围绕高频查询场景设计联合索引。

【02:25 - 02:38】
比如订单列表经常按用户状态创建时间查询，并且只展示订单号金额状态创建时间，那就可以考虑建立一个联合索引，用户ID状态创建时间、订单号金额。

【02:38 - 02:44】
这样查询时既能利用筛选条件，又能直接从索引里拿到展示字段，这就减少了大量回表。

【02:45 - 02:48】
第四种方法，先查主键再回查详情。

【02:49 - 02:50】
这个在身份页场景里特别常见。

【02:51 - 02:58】
比如你直接查第10万页的订单列表，如果直接查完整字段，麦C可能要扫描大量索引并且产生大量回表。

【02:58 - 03:12】
优化方式是，第一步先通过覆盖索引查出这一页的主件ID第二步再根据这些ID回表查完整数据，这样回表次数就从大量无效回表变成只对最终需要的几十条数据回表，这也是很多深分页优化的核心思路。

【03:13 - 03:23】
第五种方法，把冷热字段拆开，有些表里面既有高频访问字段，也有低频访问的大字段，比如用户表里面昵称、头像状态是高频字段。

【03:23 - 03:26】
个人简介，配置json备注信息是低频字段。

【03:26 - 03:41】
如果这些字段都放在一张大表里，高频查询很容易被迫读取很多不必要的数据，这时候可以考虑做垂直拆分，主表放高频字段，扩展表放低频字段，这样列表查询可以更容易被索引覆盖，也能减少无意义的回表和数据读取。

【03:42 - 03:51】
一句话总结，减少回表的本质就是让mco少做一次主键索引查询，能在二级索引里解决的就不要再回主键索引，拿整行数据。

【03:51 - 03:53】
本期视频分享到这里，我们下期再见。

【03:53 - 03:53】
拜了个拜。

generated by datatool.vip