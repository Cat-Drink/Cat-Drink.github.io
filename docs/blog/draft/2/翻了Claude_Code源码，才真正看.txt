【00:00 - 00:04】
大家好，今天来聊aiagt设计模式系列的第一篇。

【00:04 - 00:17】
Yet这个词你可能听过很多次了，今天想从它真正的起点聊起，结合原论文和Google的官方解读以及cloud code的真实源码，一起看看从理论到工程，它到底是怎么一回事。

【00:17 - 00:19】
首先，react是什么？

【00:19 - 00:27】
它来自普林斯顿大学和Google在2022年联合发表的一篇论文，exc编号，2210.03629。

【00:27 - 00:37】
React是we推理和action行动这两个词的合并，但它不是一个概念上的词，本质上是一套强迫大语言模型交替推理与行动的机制。

【00:37 - 00:42】
具体来说，他不允许模型直接给出答案，而是强制走三步。

【00:42 - 00:47】
第一步是造塔，也就是思考分析当前状态，拆解目标，规划下一步。

【00:47 - 00:51】
第二步是action，调用外部工具跟真实环境交互。

【00:51 - 00:58】
第三步是observation，读取工具返回的真实结果，作为下一轮thought的输入，这三步一直循环直到任务完成。

【00:58 - 01:01】
那原论文为什么说这是一种破局的机制？

【01:01 - 01:04】
因为在react之前有两条路都走不通。

【01:04 - 01:15】
第一条是纯推理，也就是turn of thought模型，只靠自己的参数知识推导答案，一旦遇到限定截止日期之后的信息或者需要动态查询的问题，就会产生幻觉。

【01:15 - 01:26】
第二条是纯行动模型，直接一步步执行操作，没有内部规划，遇到多条问题或者复杂任务，方向一偏就一路错到底，没有纠偏能力。

【01:26 - 01:27】
原论文在hopart。

【01:27 - 01:40】
Qa和web上当基准测试上证明了这一点，只有把推理和行动交错起来才行，推理为行动提供方向，行动的结果随时修正推理，这样模型才能在动态环境里保持准确。

【01:40 - 01:49】
我觉得这是论文最值得记住的一句话，不是react更准，而是只有交错才能纠错，静态知识加动态验证缺一不可。

【01:49 - 01:53】
Google research的官方博客从另一个角度解读了react。

【01:53 - 01:59】
Google认为，推理和行动天生是互补的，规划离不开外部验证，这是react设计的出发点。

【02:00 - 02:06】
但更重要的是，react把模型的决策过程完全透明化了，每一个thought步骤都留在上下。

【02:06 - 02:18】
文理开发者可以一步步回溯模型是怎么拆解问题的，为什么调用了这个工具，又是怎么根据一条反馈推翻了自己之前的假设勾go把这个能力叫做白合化。

【02:18 - 02:22】
这种可解析性还带来了另一个好处，可干预性。

【02:22 - 02:28】
因为推理过程可见，人类可以在任意thought节点插入修正，不需要重跑整个任务。

【02:28 - 02:33】
谷歌认为这才是让AI系统真正可信赖的工程基础理论。

【02:33 - 02:35】
说完了，我们来看工程那边是怎么写的。

【02:36 - 02:42】
Clock code的源码里query点ts文件里有一个函数叫query路，它就是react模式的字面。

【02:42 - 02:46】
实现整个函数的核心是一个while出大循环，每轮走三步。

【02:46 - 02:58】
第一步thought调用流式输出模型的推理内容，如果输出里包含了to use快needs，follow up就会变成Q如果没有工具调用循环，直接退出任务结束。

【02:58 - 03:06】
这里有个容易猜错的细节，退出原因不是in the turn，而是complete，不打开源码第1357行很难知道。

【03:07 - 03:24】
第二步，action执行run to翻到two estration文件，才发现它并不是把所有工具一起并发跑，内部有个分组逻辑，只读工具才并发，有副作用的写操作是串行执行的，这个细节在接口文档里没有说明，只有打开源码才能看到。

【03:25 - 03:34】
第三步，observation工具执行完毕后，assistant messages和tool result一起追加到mesges数组，推动下一次循环开始。

【03:34 - 03:42】
这里还有个细节，不只是tour result进去，assistant messages也要一起带上，少了这个上下文就不完整了。

【03:42 - 03:47】
这些细节都不是文档里写的，是打开源码一行一行对着看才发现的。

【03:47 - 03:51】
这个循环不是示意图，而是生产代码里真实跑着的结构。

【03:51 - 03:59】
Cross co支撑这个循环的工具超过40个按键则分5层，执行层有Basto和Ripper to直接操作环境。

【03:59 - 04:04】
文件层有file read to、File Edit to、Crato读写代码库。

【04:04 - 04:08】
信息层有web search tool和web search tool获取外部知识。

【04:09 - 04:15】
编排层有agent to，可以启动子agent扩展层，通过mcp协议接入任意外部系统。

【04:15 - 04:25】
每一个工具的输出都是react循环下一轮的observation 5层工具构成了react循环里行动这一步的全部实现。

【04:25 - 04:27】
综合下来react到底是什么？

【04:27 - 04:43】
说到底react只做了一件事，让推理和行动交替进行，而不是分开走，这一条让大模型从只能回答问题变成了能在真实世界里完成任务，从论文层看，它是让大语言模型在动态环境里边推理边纠错的机制。

【04:43 - 04:47】
从研究层看，它是把黑盒决策过程白合化的工具。

【04:47 - 04:55】
从工程层看，它是cloud code的query loo函数里那个while循环，也就是agt sdk的调度引擎react。

【04:55 - 05:05】
从2022年1篇实页的论文到今天每一次code的任务执行，核心结构从未改变，thought、action、observation三步闭环，循环驱动。

【05:05 - 05:13】
本期内容就到这里了，想了解更多细节的同学可以搜索原论文和谷博客点个关注不迷路，我们下期见。

generated by datatool.vip