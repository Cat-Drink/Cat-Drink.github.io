【00:00 - 00:04】
面试官突然问一句，为什么你项目要用release，你们也没什么并发？

【00:04 - 00:08】
这个问题千万别上来就说因为re快这个回答太浅了。

【00:08 - 00:16】
真正的关键是你要告诉面试官，我们用re不只是为了扛高并发，而是为了解决系统里的性能、状态、流量和体验问题。

【00:16 - 00:19】
我是小哲，点赞收藏加关注，我们马上开始。

【00:19 - 00:20】
想学习更多内容可以查看橱窗。

【00:21 - 00:40】
首先，如果项目真的没什么访问量，read不是必须的，数据库也能跑，但是在实际项目里用不用re不只看并发高不高，还要看有没有这些场景，有没有热点数据反复查询，有没有登录态和验证码这种短期数据，有没有接口需要限流，有没有排行榜计数器、分布式锁延迟过期这些需求。

【00:40 - 00:54】
所以re更像是一个高性能的内存组件，不是只有大厂高并发项目才能用，比如第一个场景就是缓存热点数据，像商品详情、首页配置、字典表、用户权限、系统参数，这些数据读多写少。

【00:54 - 00:57】
如果每次请求都查数据库，其实很浪费。

【00:57 - 01:00】
哪怕并发不高，数据库也不应该承担这种重复查询压力。

【01:01 - 01:05】
我们把这些数据放到release里，接口响应会更快，数据库压力也更小。

【01:05 - 01:09】
这个时候release的价值不是救命，而是优化系统结构。

【01:10 - 01:12】
第二个场景是存短生命周期的数据。

【01:12 - 01:17】
比如短信验证码、邮箱验证码、登录token图形验证码、临时授权码。

【01:18 - 01:21】
这类数据有一个共同特点，就是过一段时间自动失效。

【01:21 - 01:25】
如果放数据库就要设计过期字段，还要定时清理，逻辑会变重。

【01:25 - 01:34】
但是release天然支持过期时间，写进去的时候设置三分钟、五分钟、半小时到点自动失效，非常适合这种临时状态管理。

【01:34 - 01:37】
第三个场景是做分布式环境下的共享状态。

【01:38 - 01:51】
单机项目里很多东西可以放本地内存，但项目一旦部署多台机器，本地内存就不可靠了，比如用户登录状态、接口访问次数密等token重复提交标记，如果只存在某一台服务器里，请求打到另一台机器就读不到。

【01:52 - 01:57】
所以re可以作为多实例之间共享状态的地方，解决多服务部署下的数据一致访问问题。

【01:58 - 02:21】
第四个场景是做限流和防刷，就算系统整体并发不高，也可能存在局部风险，比如登录接口被刷、验证码接口被刷、提交订单按钮被疯狂点击，某个用户短时间内重复请求，这个时候我们可以用read记录用户或者I批在一段时间内的访问次数，比如一分钟最多请求十次，超过就拒绝，这个不是为了追求高并发，而是为了保护接口。

【02:21 - 02:35】
第五个场景是做分布式锁和密等控制，比如用户支付回调、订单状态变更、优惠券领取、库存扣减，这些逻辑不一定每天都有巨大并发，但一旦发生重复提交或者并发竞争，就可能造成脏数据。

【02:35 - 02:41】
Reis可以用来加一把轻量级的分布式锁，或者存一个请求流水号，保证同一笔业务只处理一次。

【02:42 - 03:00】
这里re解决的是一致性风险，而不只是性能问题，当然还要补一句，不是所有数据都适合放，re我们一般只放读多写少，允许短时间不一致或者天然需要过期的数据，核心交易数据强，一致数据还是要以数据库为准，radi只是加速层和状态层，不应该替代数据库。

【03:01 - 03:11】
最后总结一下，re不是只有高并发项目才需要，它本质上解决的是四类问题，缓存、热点、数据管理、临时状态共享、分布式状态保护、关键接口。

【03:11 - 03:18】
面试里你只要把这个逻辑讲清楚，面试官就知道你不是为了用技术而用技术，而是真的理解re在项目里的工程价值。

generated by datatool.vip