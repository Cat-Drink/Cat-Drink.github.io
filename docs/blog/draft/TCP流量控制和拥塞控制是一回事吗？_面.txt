【00:00 - 00:04】
面试官问你tcp为什么既有流量控制又有佣色控制？

【00:04 - 00:07】
很多人第一反应是不都是控制发送速度吗？

【00:07 - 00:09】
其实这两个机制完全不是一回事。

【00:10 - 00:15】
流量控制解决的是接收方吃不消的问题，拥色控制解决的是网络扛不住的问题。

【00:16 - 00:20】
我是小哲，点赞收藏加关注，我们马上开始更多内容查看橱窗。

【00:20 - 00:28】
先说流量控制，你可以把tcp通信想象成两个人传纸条，发送方负责写纸条，接收方负责看纸条。

【00:28 - 00:34】
如果发送方写的特别快，而接收方看的特别慢，很快桌子上就堆满了纸条，最后根本来不及处理。

【00:35 - 00:40】
网络里也是一样，接收方会给发送方一个缓冲区，用来暂存收到的数据。

【00:40 - 00:44】
如果发送速度远远大于处理速度，缓冲区就会被塞满，后续数据只能丢弃。

【00:45 - 00:47】
所以tcp设计了流量控制机制。

【00:48 - 00:52】
核心思想非常简单，接收方告诉发送方我还能接收多少数据。

【00:52 - 00:58】
这个信息记录在tcp报文里的窗口大小也叫接收窗口，英文叫receive window。

【00:58 - 01:12】
发送方每次发送的数据量不能超过接收方通告的窗口大小，如果窗口变小，发送方就减速，如果窗口变大，发送方就提速，本质上这是发送方和接收方之间的协调机制，目的是避免接收方被称爆。

【01:12 - 01:13】
那什么是佣色控制呢？

【01:14 - 01:17】
拥塞控制关注的不是接收方，而是整个网络。

【01:17 - 01:28】
还是刚才那个传纸条的例子，假设接收方处理能力很强，但中间负责传递纸条的人只有一个，如果所有人都疯狂塞纸条给他他很快就会忙不过来，最终导致大量纸条丢失。

【01:29 - 01:40】
网络也是一样，路由器、交换机、链路带宽这些资源都是有限的，当大量主机同时发送数据时，网络中的队列会越来越长，最终发生丢包，这就是网络拥塞。

【01:40 - 01:49】
为了避免把网络打爆，tcp引入了拥塞控制，它维护了一个叫拥塞窗口的变量，英文叫congtion window，简称C W N D。

【01:50 - 01:54】
发送方真正能发送的数据量不仅要看接收窗口，还要看拥塞窗口。

【01:54 - 01:58】
实际发送窗口等于接收窗口和拥塞窗口中的较小值。

【01:58 - 02:09】
也就是说，就算接收方还能接收100安币数据，如果网络当前非常拥堵，拥塞窗口只有十兆，那发送方也只能发十安B那tcp怎么知道网络是否拥塞呢？

【02:10 - 02:24】
答案是通过丢包来判断，因为tcp协议本身看不到整个网络状态，所以只能通过结果反推过程，如果发现超时重传收到大量重复，a就会认为网络可能已经拥塞了，于是主动降低发送速率。

【02:24 - 02:30】
为了实现这一点，tp设计了四个经典算法，慢启动、拥塞，避免快速重传和快速恢复。

【02:31 - 02:35】
慢启动阶段，发送窗口从很小开始，每收到一个ac就快速增长。

【02:36 - 02:44】
这样做是为了先试探网络容量，等窗口增长到一定程度后，进入拥色避免阶段，此时增长速度变慢，避免把网络一下撑爆。

【02:45 - 02:53】
如果出现丢包，说明网络已经接近极限，就触发快速重传和快速恢复，主动降低发送速率，然后重新探测网络容量。

【02:53 - 03:02】
所以总结一下，流量控制解决的是发送方和接收方之间的速度匹配问题，防止接收方缓冲区被塞满，它依赖的是接收窗口。

【03:02 - 03:13】
拥塞控制解决的是整个网络资源不足的问题，防止网络出现大规模丢包，它依赖的是拥塞窗口，一个关注终点，一个关注路途，一个保护接收方，一个保护网络。

【03:13 - 03:15】
本期视频就到这里，我们下期再见。

【03:15 - 03:16】
拜了个拜。

generated by datatool.vip