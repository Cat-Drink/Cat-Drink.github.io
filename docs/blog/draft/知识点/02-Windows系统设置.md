# Windows 系统设置

## 通过命令行激活高性能电源方案

若图形界面无法显示高性能计划，或需要远程部署，可使用命令行激活：

1. 在任务栏搜索框中输入 **"cmd"**
2. 右键点击"命令提示符"，选择 **"以管理员身份运行"**
3. 输入以下命令并按回车：

```
powercfg -setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
```

4. 命令执行成功后，高性能模式将立即激活。可输入 `powercfg -getactivescheme` 验证当前活动方案。
